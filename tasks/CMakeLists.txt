# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(depth_map_preprocessingTaskLib)
ADD_LIBRARY(${DEPTH_MAP_PREPROCESSING_TASKLIB_NAME} SHARED 
    ${DEPTH_MAP_PREPROCESSING_TASKLIB_SOURCES})
add_dependencies(${DEPTH_MAP_PREPROCESSING_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${DEPTH_MAP_PREPROCESSING_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${DEPTH_MAP_PREPROCESSING_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${DEPTH_MAP_PREPROCESSING_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${DEPTH_MAP_PREPROCESSING_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${DEPTH_MAP_PREPROCESSING_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${DEPTH_MAP_PREPROCESSING_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${DEPTH_MAP_PREPROCESSING_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${DEPTH_MAP_PREPROCESSING_TASKLIB_HEADERS}
    DESTINATION include/orocos/depth_map_preprocessing)

